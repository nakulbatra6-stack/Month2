Words → converted into vectors (numbers)
Similar meaning → vectors are close

Example:

king  → [0.25, 0.98, ...]
queen → [0.27, 0.95, ...]

This allows machines to understand meaning mathematically

pip install sentence-transformers

imp --> This code converts human language into machine-understandable numerical meaning.